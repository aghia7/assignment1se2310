package models;

public class Point {
    private double x;
    private double y;

    // constructor having both parameters
    public Point(double x, double y) {
        // do smth with parameters
    }

    // distance(Point dest) - returns the distance from current point to the dest

    // toString()
}
